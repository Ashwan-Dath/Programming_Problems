// 202. Happy Number

// Write an algorithm to determine if a number n is happy.

// A happy number is a number defined by the following process:

// Starting with any positive integer, replace the number by the sum of the squares of its digits.
// Repeat the process until the number equals 1 (where it will stay), or it loops endlessly in a cycle which does not include 1.
// Those numbers for which this process ends in 1 are happy.
// Return true if n is a happy number, and false if not.

 

// Example 1:

// Input: n = 19
// Output: true
// Explanation:
// 12 + 92 = 82
// 82 + 22 = 68
// 62 + 82 = 100
// 12 + 02 + 02 = 1

// Example 2:
// Input: n = 2
// Output: false
 

// Constraints:
// 1 <= n <= 231 - 1

public class Main
{
    
    public static int sqrNum(int n){
        int sum = 0;
        while(n != 0){
            int d = n % 10;
            sum = sum + (d * d);
            n = n/10;
        }
        return sum;
    }
    
    public static boolean isHappy(int n) {

        // Explanation:
        // This method checks whether a number is a Happy Number using Floyd's
        // Cycle Detection Algorithm (Slow and Fast Pointer).
        //
        // A Happy Number is a number where repeatedly replacing the number with
        // the sum of the squares of its digits eventually reaches 1.
        //
        // Example:
        // 19 -> 1² + 9² = 82
        // 82 -> 8² + 2² = 68
        // 68 -> 6² + 8² = 100
        // 100 -> 1² + 0² + 0² = 1
        // Therefore, 19 is a Happy Number.
        //
        // sqrNum(n):
        // - Extracts each digit using n % 10.
        // - Adds the square of each digit to sum.
        // - Removes the last digit using n / 10.
        // - Returns the sum of squares of the digits.
        //
        // isHappy(n):
        // - slow moves one step at a time.
        // - fast moves two steps at a time.
        // - If the sequence reaches 1, the number is Happy.
        // - If slow and fast meet at a value other than 1, a cycle exists,
        //   so the number is not Happy.
        //
        // Why does cycle detection work?
        // Every number eventually becomes a relatively small number after
        // repeatedly calculating the sum of squares of its digits.
        // Therefore, the sequence either reaches 1 or enters a cycle.
        // Floyd's algorithm detects that cycle without using extra memory.
        //
        // Time Complexity:
        // O(log n)
        //
        // sqrNum() processes all digits of n, so it takes O(log n).
        // The cycle detection also performs a bounded number of operations,
        // giving an overall complexity of O(log n).
        //
        // Space Complexity:
        // O(1)
        //
        // We only use a few integer variables (slow, fast, sum, d),
        // so no extra data structure is required.

        int slow = n;
        int fast = n;
        
        while(fast != 1){
            slow = sqrNum(slow);
            fast = sqrNum(fast);
            fast = sqrNum(fast);
            System.out.println("slow " + slow + " fast " + fast );
            if(slow == fast && slow != 1){
                return false;
            }
        }
        System.out.println(slow + " " + fast);
        
        return true;









// -------------------------------------------------------------------------------

        // APPROACH: 1 using Extra space.
        // We need to repeatedly replace n with the sum of the squares
        // of its digits.
        //
        // Example of one transformation:
        // n = 19
        // 1² + 9² = 82
        //
        // Then we repeat the same process with 82:
        // 8² + 2² = 68
        //
        // We continue this process until:
        // 1. The sum becomes 1 -> Happy Number -> return true
        // 2. A number that we have already seen appears again
        //    -> a cycle exists -> return false
        //
        // To detect a cycle, we use a HashSet.
        //
        // HashSet<Integer> store = new HashSet<>();
        //
        // Before continuing with a new sum, we try to add it to the HashSet.
        //
        // if (!store.add(sum)) {
        //     return false;
        // }
        //
        // HashSet.add() returns:
        // true  -> sum was not present, so it is added.
        // false -> sum was already present, so a cycle exists.
        //
        // Once sum == 1, we return true.
        //
        //
        // TIME COMPLEXITY:
        //
        // Let D be the number of digits in n.
        // Calculating the sum of the squares of the digits takes O(D).
        //
        // We repeat this process K times until we reach 1 or detect a cycle.
        //
        // Therefore, the precise complexity is:
        //
        // Time: O(D * K)
        //
        // Since D = O(log n), this is commonly expressed as:
        //
        // Time: O(log n)     [for this problem, K is bounded]
        //
        //
        // SPACE COMPLEXITY:
        //
        // We use a HashSet to store the previously seen sums.
        //
        // If K different values are stored:
        //
        // Space: O(K)
        //
        // For this problem, the sequence eventually becomes bounded
        // and enters either 1 or a cycle.
        //
        // Commonly:
        //
        // Space: O(log n)

        // HashSet<Integer> store = new HashSet<>();
        // boolean b = false;
        // int dup = n;
        // while(n != 1){
        //     System.out.println("dup " + dup);
        //     int sum = 0;
        //     while(n != 0){
        //         int rev = n % 10;
        //         System.out.println("inside rev " + rev);
        //         sum = sum + (rev * rev);
        //         n /= 10;
        //     }
        //     System.out.println("before sum " + sum);
            
            
        //     // 
        //     // if(store.contains(sum)){
        //     //     return false;
        //     // }
        //     // store.add(sum);
            
        //     System.out.println("store " + store);
            
        //     if (!store.add(sum)) {
        //         return false;
        //     }
            
            
        //     if(sum == 1){
        //         return true;
        //     }
        //     else{
        //         n = sum;
        //     }
        //     System.out.println("after sum " + sum);
        // }
        // return false;
    }

	public static void main(String[] args) {
	    int num = 10;
	    boolean b = isHappy(num);
	    System.out.println(b);
	}
}
